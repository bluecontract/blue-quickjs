# test-harness

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build test-harness` to build the library.

## Running unit tests

Run `nx test test-harness` to execute the unit tests via [Vitest](https://vitest.dev/).
