# quickjs-wasm-build

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build quickjs-wasm-build` to build the library.

## Running unit tests

Run `nx test quickjs-wasm-build` to execute the unit tests via [Vitest](https://vitest.dev/).
