# quickjs-runtime

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build quickjs-runtime` to build the library.

## Running unit tests

Run `nx test quickjs-runtime` to execute the unit tests via [Vitest](https://vitest.dev/).
