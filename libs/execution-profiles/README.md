# @blue-quickjs/execution-profiles

Canonical execution-profile registry shared by runtime and builder surfaces.

Exports:

- profile identifiers (`baseline-v1`, `compat-general-v1`,
  `compat-binary-v1`)
- capability lookup helpers
- profile membership checks
