# smoke-node

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build smoke-node` to build the library.

## Running unit tests

Run `nx test smoke-node` to execute the unit tests via [Vitest](https://vitest.dev/).
